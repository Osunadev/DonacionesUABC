<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.donacionesuabc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivitiesLoggedIn.PaqueteDonaciones.ModificarDonacion"></activity>
        <activity android:name=".ActivitiesLoggedIn.PaqueteDonaciones.EstadoDonacion" />
        <activity android:name=".ActivitiesLoggedIn.PaqueteDonaciones.PublicacionesActivasDonacion" />
        <activity android:name=".Intercambios" /> <!-- DO NO FORGET TO DECLARE ACTIVITIES HERE -->
        <activity android:name=".ActivitiesLoggedIn.PaqueteDonaciones.SubirDonacion" />
        <activity android:name=".ActivitiesLoggedIn.PaqueteDonaciones.Donaciones" /> <!-- DO NO FORGET TO DECLARE ACTIVITIES HERE -->
        <activity android:name=".VerArticulos" />
        <activity android:name=".VistaArticulo" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivitiesLoggedIn.MenuLoggedActivity" />
    </application>

</manifest>